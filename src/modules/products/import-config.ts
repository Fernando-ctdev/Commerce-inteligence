/* Chave da importação URL-first (SPEC Slice 012 / ADR-028). false = fluxo
   desativado: o endpoint responde indisponível e a UI não oferece a ação.
   Reabilitar exige decisão documentada e reativar os testes pulados. */
export const URL_IMPORT_ENABLED = false;
